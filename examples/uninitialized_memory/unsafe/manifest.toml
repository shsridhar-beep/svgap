schema_version = "1.0"
candidate_id = "uninitialized-memory-unsafe"
[design]
top = "uninitialized_memory"
sources = ["design.sv"]
[functional]
commands = [["iverilog", "-g2012", "-o", "${SVGAP_BUILD}/sim.vvp", "design.sv", "../tb.sv"], ["vvp", "${SVGAP_BUILD}/sim.vvp"]]
[structural]
backend = "reference-yosys"
[intent]
memory_power_on = "initialized_or_reset"
[[intent.clocks]]
name = "core"
port = "clk"
[output]
report = "build/report.json"
