schema_version = "2.0"
candidate_id = "synthesis-directive-equivalence-safe"

[design]
top = "arithmetic_unit"
sources = ["design.sv"]

[functional]
commands = [
  ["iverilog", "-g2012", "-o", "${SVGAP_BUILD}/sim.vvp", "design.sv", "../tb.sv"],
  ["vvp", "${SVGAP_BUILD}/sim.vvp"],
]

[[oracles]]
id = "synthesized-equivalence"
class = "equivalence"
backend = "equivalence-yosys"
contributes_to_gap = true
required = true

[oracles.options]
reference_sources = ["reference.sv"]
reference_top = "arithmetic_unit"
depth = 1
rule_id = "REF-EQUIV-001"
message = "synthesis directives make the candidate inequivalent to the reference"

[[oracles]]
id = "ordinary-lint"
class = "lint"
backend = "lint-verilator"
contributes_to_gap = false
required = false

[oracles.options]
extra_args = ["-Wno-DECLFILENAME"]

[intent]

[output]
report = "build/report.json"
